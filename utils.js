const sind = (angle) => {
    return Math.sin(angle * (Math.PI/180));
}

const cosd = (angle) => {
    return Math.cos(angle * (Math.PI/180));
}

const tand = (angle) => {
    return Math.tan(angle * (Math.PI/180));
}